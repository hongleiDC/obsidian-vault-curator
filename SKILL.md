---
name: obsidian-vault-curator
description: 整理、重构、美化和维护 Obsidian Markdown 笔记与 Vault，尤其适用于笔记长期保存在 GitHub 仓库中的个人知识库。用于从持久化 Vault 配置自动定位 GitHub 笔记仓库、读取或写回单篇/多篇 .md、结构重排、Frontmatter/Properties 合并、tags/aliases 管理、wikilinks/嵌入/callout/任务/引用规范化、概念解释补丁、批量知识关联与安全提交。必须保留事实、代码、公式、附件、任务、引用、块 ID、Dataview 字段和已有链接；GitHub 写入必须使用新鲜 SHA、串行写入、有限重试和自适应分支策略，避免持续冲突或重复报错。
---

# Obsidian Vault Curator

把“有内容但缺结构”的 Obsidian 笔记整理成简洁、可检索、可关联、可长期维护的知识库内容。优先保护信息和 Obsidian 语义，再改善结构与可读性；不要为了好看而制造层级、标签或链接。

本 Skill 的主要使用场景是：**Obsidian Vault 长期保存在 GitHub 仓库中，ChatGPT 通过 GitHub connector 读取、整理并安全写回。** 同时保留对用户直接上传 Markdown 或本地 Vault 的支持。

详细规范见：
- `references/github-backend.md`：GitHub 持久化配置、读取、写回、分支、PR、冲突和重试策略。
- `references/formatting-rules.md`：排版、Properties、任务、代码、公式、callout 等规则。
- `references/vault-linking.md`：Vault 索引、真实双链、批量整理与文件重命名规则。
- `references/examples.md`：典型输入/输出示例。
- `config/vault.example.yaml`：个人 Vault 配置模板。

## 工作模式

先判断任务属于哪一种模式，然后直接执行；除非用户明确要求，否则不要把简单整理变成复杂知识工程。

1. **GitHub 单篇整理**：从已绑定的 GitHub Vault 读取一篇笔记，整理后安全写回。
2. **GitHub 批量整理**：在临时分支中处理多篇笔记、跨笔记链接或元数据一致性，再通过 PR 合并。
3. **单篇文件整理**：整理用户直接提供的 Markdown 或本地 `.md` 文件。
4. **概念补丁**：只解释用户点名的不清楚术语，不重新整理整篇。
5. **Vault / 多篇整理**：扫描多篇笔记，验证真实链接后再建立关联。
6. **审计模式**：只指出结构、链接、标签、元数据或语法问题，不写回正文。

## GitHub Vault 是默认长期后端

### 区分两个仓库

始终区分：

- **Skill source repository**：`hongleiDC/obsidian-vault-curator`，只存放 Skill 本身。
- **Vault repository**：用户实际保存 Obsidian 笔记的 GitHub 仓库。

除非用户明确说笔记也在 Skill source repository 中，否则绝不要把笔记整理结果写入 `hongleiDC/obsidian-vault-curator`。

### 持久化 Vault 配置

优先读取用户私有配置 `config/vault.yaml`。它至少保存：

```yaml
vault:
  repository: owner/repository
  branch: main
  root: ""
```

还可以保存写回策略、批量大小、自动合并和安全开关。完整模板见 `config/vault.example.yaml`。

规则：

1. 每次 GitHub-backed 任务开始时先读取 `config/vault.yaml`。
2. 配置存在且有效时，直接使用其中的仓库、分支和 Vault 根目录；不要重复询问用户。
3. 配置不存在时进入**一次性绑定模式**：只询问真正缺失的 Vault 仓库地址，以及必要时的分支或 Vault 子目录。
4. 一旦创建持久配置，后续新对话也应从配置恢复，不依赖聊天记忆。
5. `config/vault.yaml` 是用户私有配置，不提交到公开 Skill 仓库；公开仓库只保存 example。

如果执行环境无法持久写入 Skill 私有配置，不要假装已保存；应明确告诉用户需要重新打包一个带个人配置的 Skill，或使用其指定的私有配置存储位置。

## GitHub connector 契约

GitHub-backed 模式必须使用 GitHub connector 读取和写回仓库，不要用普通网页抓取替代已连接的私有仓库访问。

开始任务时：

1. 从持久配置得到 `repository`、`branch`、`root`。
2. 获取仓库元数据，确认仓库和分支仍可访问。
3. 所有路径都限制在配置的 Vault 根目录内，除非用户明确要求跨出该范围。
4. 编辑前重新读取远端当前文件，不依赖上次对话缓存。

完整 GitHub 操作规则见 `references/github-backend.md`。

## 自适应写回策略

### 单文件安全修改：直接写入

仅当以下条件全部成立时直接写配置的基础分支：

- 只创建或修改 1 个文本笔记；
- 不涉及重命名、移动、删除；
- 不需要同步修改其他 backlink；
- 不修改二进制附件；
- 保护检查通过。

直接写入时：

1. 读取目标文件并记录 blob SHA。
2. 在内存中一次性完成完整的新内容，不做多次零碎写入。
3. **真正写入前再次读取同一路径**。
4. SHA 未变化时，使用这次最新 SHA 更新。
5. SHA 已变化时，放弃旧写入计划，在新内容上重新应用整理逻辑，只允许重试一次。
6. 写入后重新读取验证结果。

### 多文件或结构性修改：临时分支 + PR

出现以下任一情况时，不直接写基础分支：

- 修改 2 个或更多文件；
- 建立或调整跨笔记链接；
- 跨多篇统一 Properties、标签或分类；
- 重命名、移动或删除文件；
- 更新多个 backlink；
- 其他难以作为单文件独立验证的修改。

流程：

1. 从最新基础分支创建唯一临时分支，例如 `obsidian-curator/20260829-topic`。
2. 在临时分支上**串行**处理文件；同一路径绝不并行写。
3. 每个已有文件在更新前都重新读取并使用新鲜 SHA。
4. 全部修改完成后验证保护项和链接。
5. 创建 PR 指向基础分支。
6. 普通文本整理且验证通过时，可按配置自动 squash merge。
7. 重命名、移动、删除、附件变更等高风险修改默认不自动合并，除非用户配置明确允许。

默认每批最多 10 个变更文件；更大的 Vault 整理按目录或主题拆成多个逻辑批次。

## GitHub 错误与重试规则

目标是**避免持续报错循环**。

- **stale SHA / conflict**：刷新最新文件，重新应用修改，只重试 1 次；第二次仍失败则停止该路径。
- **文件存在**：使用 update + 当前 SHA；不要反复 create。
- **文件不存在**：仅在允许创建时使用 create；不要反复 update。
- **权限 / 登录失败**：立即停止写入，不重复尝试。
- **分支名冲突**：生成一个新后缀再试 1 次，不循环尝试同名分支。
- **内容未变化**：跳过写入，不创建空提交。
- **同一路径**：禁止并行写；同一次任务优先只写一次完整内容。
- **成功后再次修改同一文件**：使用返回的新 SHA，或重新读取后再写。

不要为了“成功推上去”无限重试。出现无法自动解决的冲突时，保留远端内容并报告具体阻塞点。

## 输入与输出约定

接受以下输入：

- 已绑定 GitHub Vault 中的笔记名、路径、主题或目录；
- 用户直接粘贴的 Markdown；
- 一个或多个 `.md` 文件；
- 一个 Obsidian Vault 或其中某个目录；
- 用户指定的局部段落、标题或概念。

输出遵循：

- GitHub Vault 任务：默认以仓库当前内容为源，按写回策略提交整理结果；
- 粘贴文本：返回完整可直接替换的 Markdown；
- 本地文件任务：按用户要求写回原文件或生成整理后的副本；未明确允许覆盖时优先保留原件；
- 多文件任务：保持原目录结构；不要擅自移动或重命名文件；
- 审计任务：只给问题和建议，不改内容。

## 核心保护契约

把下面内容视为“受保护语义”。整理时允许移动到更合理的位置，但不要无意改写、删除或破坏：

- 原文中的事实、数字、结论、例子、引用与限定条件；
- YAML Frontmatter / Obsidian Properties 中已有字段；
- fenced code blocks、inline code、命令、配置片段；
- `$$...$$`、`$...$`、LaTeX 数学公式；
- `![[...]]`、Markdown 图片、附件路径与尺寸参数；
- 已有 `[[wikilinks]]`、heading links、block links；
- block ID，如 `^block-id`；
- 任务，如 `- [ ]`、`- [x]`；
- Dataview / DataviewJS 字段与表达式，如 `key:: value`；
- footnotes、引用块、HTML、Obsidian 注释 `%%...%%`；
- 已有 callout 的语义和折叠状态。

如果修改前后文件都可访问且脚本执行环境可用，完成后运行 `scripts/verify_note_preservation.py ORIGINAL CURATED`。发现保护项丢失时先修复，再写回或交付。

## 单篇整理流程

1. **完整读取**：先通读全文，不要边读边改。
2. **识别主题**：提取主问题、并列概念、步骤、因果与总结。
3. **保护语义**：标记代码、公式、嵌入、任务、引用、Properties、链接等保护对象。
4. **重建结构**：用最少层级组织内容；相关内容归组，重复内容合并但不损失信息。
5. **规范 Obsidian 语法**：合并 Properties、清理标签、保留/验证双链、必要时添加少量 callout。
6. **可读性润色**：把超长段落拆为短段落或列表，修正明显语病，但不要改变原意。
7. **质量检查**：检查 YAML、标题层级、链接、列表、任务、代码、公式、嵌入和保护项。
8. **按 GitHub 或本地写回约定输出**。

## 结构与排版原则

- 一个笔记最多一个 `#` 主标题；正文优先使用 `##` 和 `###`，仅在确有必要时使用更深层级。
- 不为每两三句话创建新标题；标题表达“主题”，不是装饰。
- 并列属性、条件、步骤、优缺点适合列表；连续论证适合短段落。
- 表格只用于真正的多维对比、参数矩阵或结构化数据。
- 高亮、加粗、斜体都要克制；不要使用 emoji 作为默认装饰。
- callout 只服务于摘要、结论、警告、问题、示例等明显语义；普通笔记通常 0–2 个即可。
- 图片/嵌入靠近其解释内容；不要改附件文件名或路径。
- 不自动添加“相关笔记”堆砌区；优先把链接嵌入有语义的正文位置。

更详细的格式边界见 `references/formatting-rules.md`。

## Properties / Frontmatter 规则

- **合并而不是覆盖**已有 Frontmatter。
- 保留未知字段及其值；不要因为模板化而删除用户自定义属性。
- `tags`、`aliases` 只做去重、规范化和必要补充。
- 不凭空填写 `source`、`author`、`created`、`modified`、`status` 等事实型属性。
- 不把正文里偶然出现的所有名词都变成标签。
- 标签优先表达稳定主题或工作流分类，而不是一次性细节。
- 若用户已有明显命名体系，沿用其体系，不另造第二套分类法。

## Wikilink 与知识关联

### 单篇且无法查看 Vault

- 保留所有现有双链。
- 不确认目标真实存在时，不新增“看起来合理”的 wikilink。
- 可以在最终说明中提出候选关联，但不要把候选直接写入正文。

### 可查看 GitHub Vault 或多篇笔记

1. 只在配置的 Vault repository 范围内解析真实目标，除非用户明确要求跨仓库。
2. 可使用 GitHub 搜索/读取确认路径、重复文件名、标题、heading、block ID、已有 links/embeds。
3. 如果 Vault 已在本地可访问，可运行 `scripts/build_vault_index.py <vault-root> --output <index.json>`。
4. 只链接真实存在且有语义关系的目标。
5. 同一概念通常只在首次或关键位置建立链接，不要每次出现都加链。
6. heading link / block link 必须确认目标 heading / block ID 存在。
7. 文件名重复时优先使用能唯一解析的路径形式。

完整规则见 `references/vault-linking.md`。

## 批量 / Vault 整理流程

1. 从 GitHub 持久配置确定仓库、分支和 Vault root。
2. 识别目标目录或主题簇，不要一开始就做全库大规模重写。
3. 使用临时分支处理批量修改。
4. 每篇先执行单篇整理流程，再建立跨笔记链接。
5. 不因追求 Graph View 密度而硬加链接。
6. 不自动创建大量新标签；优先复用已有稳定标签。
7. 不自动重命名或移动文件；如用户明确要求，先检查所有入链，再同步更新引用。
8. 批量完成后验证代表性笔记和所有高风险保护项，再创建/合并 PR。

## 概念补丁

当用户只问“这里的 XX 是什么 / 为什么 / 和 YY 有什么区别”时：

- 不重新排版整篇；
- 在原句附近添加一个默认折叠的 callout；
- 标题使用 `补丁：...`，明确区分原文与新增解释；
- 先给职责/定义，再给必要的边界或极短示例；一般 2–5 句；
- 只写可靠、通行的定义；不确定时明确说明简化或不确定性；
- 若需要外部事实才能可靠解释，先检索可靠来源再写；
- 新增 wikilink 仍必须遵守真实目标规则。

推荐格式：

```markdown
原句……

> [!question]- 补丁：XX 是什么？
> 简洁、可靠的解释。
```

## 内容补充边界

默认只做整理，不扩写知识内容。仅在以下情况增加新信息：

- 用户明确要求解释、补充、查漏或扩写；
- 为消除歧义需要极小的连接语，但不能引入新事实；
- 用户要求概念补丁。

任何新增事实都要和原文可区分；需要外部验证时先验证，不要凭记忆臆造引用、URL、数据或论文。

## 最终质量检查

交付或提交前确认：

- 使用的是持久配置中正确的 Vault repository，而不是 Skill source repository；
- 原始事实和关键限定没有被改变；
- Frontmatter 可解析，原有未知属性仍在；
- 代码、公式、附件、任务、Dataview、footnotes、block ID 没有损坏；
- 新增 wikilink 均有真实目标；
- 标题层级没有为了美观过度拆分；
- 列表、表格、callout 使用有明确理由；
- 没有生成虚假的来源、标签体系或知识关系；
- GitHub 写入使用的是当前 SHA，不复用陈旧 SHA；
- 多文件任务使用临时分支而不是连续直接写 main；
- 没有无上限重试；冲突最多按配置重试一次；
- 写入后已验证远端结果，或明确说明无法验证的原因。
